{
  "name": "WF-05 Research",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "research-v1",
        "responseMode": "onReceived",
        "options": {
          "responseCode": 202
        }
      },
      "id": "7a33b1e5-0a26-4f18-a148-765a33aab501",
      "name": "Signed Research Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [-460, 100],
      "webhookId": "7a33b1e5-0a26-4f18-a148-765a33aab502"
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst headers = $json.headers || {};\nconst payload = $json.body || {};\nconst timestamp = String(headers['x-workflow-timestamp'] || '');\nconst nonce = String(headers['x-workflow-nonce'] || '');\nconst received = String(headers['x-workflow-signature'] || '');\nconst sourcePath = '/webhook/research-v1';\nconst body = JSON.stringify(payload);\nconst digest = crypto.createHash('sha256').update(body, 'utf8').digest('hex');\nconst canonical = [timestamp, nonce, 'POST', sourcePath, digest].join('\\n');\nconst secret = $env.WORKFLOW_HMAC_SECRET;\nconst previousSecret = $env.WORKFLOW_HMAC_PREVIOUS_SECRET;\nif (!secret || !timestamp || !nonce || !received) throw new Error('Workflow signature is missing');\nconst authentic = [secret, previousSecret].filter(Boolean).some((candidate) => {\n  const expected = `sha256=${crypto.createHmac('sha256', candidate).update(canonical, 'utf8').digest('hex')}`;\n  return received.length === expected.length && crypto.timingSafeEqual(Buffer.from(received), Buffer.from(expected));\n});\nif (!authentic) throw new Error('Workflow signature is invalid');\nif (Math.abs(Math.floor(Date.now() / 1000) - Number(timestamp)) > 300) throw new Error('Workflow signature expired');\nif (payload.contractVersion !== '1.0' || !payload.correlationId || !payload.idempotencyKey || !payload.actorId || !payload.brandId || !payload.opportunityId || !payload.requestedAt) throw new Error('Research contract is invalid');\nconst targetPath = '/api/internal/workflows/recovery/execute';\nconst recoveryPayload = {\n  contractVersion: '1.0',\n  workflowExecutionId: String($execution.id),\n  workflowName: 'WF-05 Research',\n  target: 'research',\n  requestPayload: payload,\n};\nconst recoveryBody = JSON.stringify(recoveryPayload);\nconst nextTimestamp = String(Math.floor(Date.now() / 1000));\nconst nextNonce = crypto.randomUUID();\nconst nextDigest = crypto.createHash('sha256').update(recoveryBody, 'utf8').digest('hex');\nconst nextCanonical = [nextTimestamp, nextNonce, 'POST', targetPath, nextDigest].join('\\n');\nconst signature = `sha256=${crypto.createHmac('sha256', secret).update(nextCanonical, 'utf8').digest('hex')}`;\nreturn [{ json: { body: recoveryBody, nonce: nextNonce, signature, timestamp: nextTimestamp, url: `${$env.APP_BASE_URL}${targetPath}` } }];"
      },
      "id": "7a33b1e5-0a26-4f18-a148-765a33aab503",
      "name": "Verify and Sign Research Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [-220, 100]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "accept-encoding",
              "value": "identity"
            },
            {
              "name": "content-type",
              "value": "application/json"
            },
            {
              "name": "x-workflow-name",
              "value": "WF-05 Research"
            },
            {
              "name": "x-workflow-timestamp",
              "value": "={{ $json.timestamp }}"
            },
            {
              "name": "x-workflow-nonce",
              "value": "={{ $json.nonce }}"
            },
            {
              "name": "x-workflow-signature",
              "value": "={{ $json.signature }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ $json.body }}",
        "options": {
          "response": {
            "response": {
              "neverError": false,
              "responseFormat": "file",
              "outputPropertyName": "data"
            }
          },
          "timeout": 200000
        }
      },
      "id": "7a33b1e5-0a26-4f18-a148-765a33aab504",
      "name": "Run and Persist Bounded Research",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [20, 100]
    },
    {
      "parameters": {
        "operation": "fromJson",
        "options": {}
      },
      "id": "7a33b1e5-0a26-4f18-a148-765a33aab506",
      "name": "Decode Research Result",
      "type": "n8n-nodes-base.extractFromFile",
      "typeVersion": 1,
      "position": [240, 100]
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst research = $input.first().json.data;\nconst request = $('Signed Research Webhook').first().json.body;\nif (research?.contractVersion !== '1.0' || !research.researchRunId || !research.generationRunId || typeof research.readyForWriting !== 'boolean') throw new Error('Research result contract is invalid');\nif (!research.readyForWriting) return [{ json: { dispatch: false, reason: 'research_not_ready_for_writing' } }];\nconst secret = $env.WORKFLOW_HMAC_SECRET;\nconst baseUrl = $env.N8N_WEBHOOK_BASE_URL;\nif (!secret || !baseUrl) throw new Error('Editorial preparation environment is not configured');\nconst path = '/webhook/editorial-generation-v1';\nconst styles = ['newsworthy_authority', 'educational_breakdown', 'perspective_conversation'];\nreturn styles.map((contentStyle) => {\n  const payload = {\n    contractVersion: '1.0',\n    correlationId: request.correlationId,\n    idempotencyKey: `rss-auto-drafts:${request.opportunityId}:${contentStyle}`,\n    actorId: request.actorId,\n    brandId: request.brandId,\n    opportunityId: request.opportunityId,\n    contentStyles: [contentStyle],\n    tone: 'thoughtful',\n    requestedAt: new Date().toISOString(),\n  };\n  const body = JSON.stringify(payload);\n  const timestamp = String(Math.floor(Date.now() / 1000));\n  const nonce = crypto.randomUUID();\n  const digest = crypto.createHash('sha256').update(body, 'utf8').digest('hex');\n  const canonical = [timestamp, nonce, 'POST', path, digest].join('\\n');\n  const signature = `sha256=${crypto.createHmac('sha256', secret).update(canonical, 'utf8').digest('hex')}`;\n  return { json: { dispatch: true, body, contentStyle, nonce, signature, timestamp, url: `${baseUrl}${path}` } };\n});"
      },
      "id": "7a33b1e5-0a26-4f18-a148-765a33aab507",
      "name": "Sign Three-style Draft Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 100]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "c050f001-6a77-4a0f-8d1c-000000000014",
              "leftValue": "={{ $json.dispatch }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "c050f001-6a77-4a0f-8d1c-000000000014",
      "name": "Dispatch Editorial Generation?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [680, 100]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "accept-encoding",
              "value": "identity"
            },
            {
              "name": "content-type",
              "value": "application/json"
            },
            {
              "name": "x-workflow-name",
              "value": "WF-05 Research"
            },
            {
              "name": "x-workflow-timestamp",
              "value": "={{ $json.timestamp }}"
            },
            {
              "name": "x-workflow-nonce",
              "value": "={{ $json.nonce }}"
            },
            {
              "name": "x-workflow-signature",
              "value": "={{ $json.signature }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ $json.body }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": false
            }
          },
          "timeout": 30000
        }
      },
      "id": "7a33b1e5-0a26-4f18-a148-765a33aab508",
      "name": "Generate Three Draft Styles",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [900, 100]
    }
  ],
  "pinData": {},
  "connections": {
    "Signed Research Webhook": {
      "main": [
        [
          {
            "node": "Verify and Sign Research Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verify and Sign Research Request": {
      "main": [
        [
          {
            "node": "Run and Persist Bounded Research",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run and Persist Bounded Research": {
      "main": [
        [
          {
            "node": "Decode Research Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Decode Research Result": {
      "main": [
        [
          {
            "node": "Sign Three-style Draft Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sign Three-style Draft Request": {
      "main": [
        [
          {
            "node": "Dispatch Editorial Generation?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Dispatch Editorial Generation?": {
      "main": [
        [
          {
            "node": "Generate Three Draft Styles",
            "type": "main",
            "index": 0
          }
        ],
        []
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": true,
    "executionTimeout": 900
  },
  "versionId": "7a33b1e5-0a26-4f18-a148-765a33aab505",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "tags": []
}

{
  "name": "WF-02 Manual Intake",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "manual-input-intake-v1",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "2f974b26-f2ac-44fa-a67f-acde65873301",
      "name": "Signed Manual Intake Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [-460, 100],
      "webhookId": "2ecf8e3e-45ef-4c17-bf90-acde65873302"
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst headers = $json.headers || {};\nconst payload = $json.body || {};\nconst timestamp = String(headers['x-workflow-timestamp'] || '');\nconst nonce = String(headers['x-workflow-nonce'] || '');\nconst received = String(headers['x-workflow-signature'] || '');\nconst sourcePath = '/webhook/manual-input-intake-v1';\nconst body = JSON.stringify(payload);\nconst digest = crypto.createHash('sha256').update(body, 'utf8').digest('hex');\nconst canonical = [timestamp, nonce, 'POST', sourcePath, digest].join('\\n');\nconst secret = $env.WORKFLOW_HMAC_SECRET;\nconst previousSecret = $env.WORKFLOW_HMAC_PREVIOUS_SECRET;\nif (!secret || !timestamp || !nonce || !received) throw new Error('Workflow signature is missing');\nconst authentic = [secret, previousSecret].filter(Boolean).some((candidate) => {\n  const expected = `sha256=${crypto.createHmac('sha256', candidate).update(canonical, 'utf8').digest('hex')}`;\n  return received.length === expected.length && crypto.timingSafeEqual(Buffer.from(received), Buffer.from(expected));\n});\nif (!authentic) throw new Error('Workflow signature is invalid');\nif (Math.abs(Math.floor(Date.now() / 1000) - Number(timestamp)) > 300) throw new Error('Workflow signature expired');\nif (payload.contractVersion !== '1.0' || !payload.sourceDocumentId || !payload.correlationId || !payload.idempotencyKey) throw new Error('Manual intake contract is invalid');\nconst targetPath = '/webhook/source-normalize-v1';\nconst nextTimestamp = String(Math.floor(Date.now() / 1000));\nconst nextNonce = crypto.randomUUID();\nconst nextDigest = crypto.createHash('sha256').update(body, 'utf8').digest('hex');\nconst nextCanonical = [nextTimestamp, nextNonce, 'POST', targetPath, nextDigest].join('\\n');\nconst signature = `sha256=${crypto.createHmac('sha256', secret).update(nextCanonical, 'utf8').digest('hex')}`;\nreturn [{ json: { body, nonce: nextNonce, signature, timestamp: nextTimestamp, url: `${$env.N8N_WEBHOOK_BASE_URL}${targetPath}` } }];"
      },
      "id": "6835d06d-b0f9-405a-b275-acde65873303",
      "name": "Verify and Sign Normalize Handoff",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [-220, 100]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "accept-encoding",
              "value": "identity"
            },
            {
              "name": "content-type",
              "value": "application/json"
            },
            {
              "name": "x-workflow-name",
              "value": "WF-02 Manual Intake"
            },
            {
              "name": "x-workflow-timestamp",
              "value": "={{ $json.timestamp }}"
            },
            {
              "name": "x-workflow-nonce",
              "value": "={{ $json.nonce }}"
            },
            {
              "name": "x-workflow-signature",
              "value": "={{ $json.signature }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ $json.body }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "b3c37b29-d20b-4ddc-a211-acde65873304",
      "name": "Start Normalize Workflow",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [20, 100]
    }
  ],
  "pinData": {},
  "connections": {
    "Signed Manual Intake Webhook": {
      "main": [
        [
          {
            "node": "Verify and Sign Normalize Handoff",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verify and Sign Normalize Handoff": {
      "main": [
        [
          {
            "node": "Start Normalize Workflow",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": true,
    "executionTimeout": 300
  },
  "versionId": "595b3607-1426-4138-8ff8-acde65873305",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "tags": []
}

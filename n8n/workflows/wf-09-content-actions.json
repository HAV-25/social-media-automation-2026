{
  "name": "WF-09 Content Actions",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "content-actions-v1",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "bd77f529-4e60-425c-a58c-ba9e77efe901",
      "name": "Signed Content Action Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [-460, 100],
      "webhookId": "bd77f529-4e60-425c-a58c-ba9e77efe902"
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst headers = $json.headers || {};\nconst payload = $json.body || {};\nconst timestamp = String(headers['x-workflow-timestamp'] || '');\nconst nonce = String(headers['x-workflow-nonce'] || '');\nconst received = String(headers['x-workflow-signature'] || '');\nconst sourcePath = '/webhook/content-actions-v1';\nconst sourceBody = JSON.stringify(payload);\nconst digest = crypto.createHash('sha256').update(sourceBody, 'utf8').digest('hex');\nconst canonical = [timestamp, nonce, 'POST', sourcePath, digest].join('\\n');\nconst secret = $env.WORKFLOW_HMAC_SECRET;\nconst previousSecret = $env.WORKFLOW_HMAC_PREVIOUS_SECRET;\nif (!secret || !timestamp || !nonce || !received) throw new Error('Workflow signature is missing');\nconst authentic = [secret, previousSecret].filter(Boolean).some((candidate) => {\n  const expected = `sha256=${crypto.createHmac('sha256', candidate).update(canonical, 'utf8').digest('hex')}`;\n  return received.length === expected.length && crypto.timingSafeEqual(Buffer.from(received), Buffer.from(expected));\n});\nif (!authentic) throw new Error('Workflow signature is invalid');\nif (Math.abs(Math.floor(Date.now() / 1000) - Number(timestamp)) > 300) throw new Error('Workflow signature expired');\nconst commonValid = payload.contractVersion === '1.0' && payload.correlationId && payload.idempotencyKey && payload.actorId && payload.brandId && payload.postDraftId && payload.expectedVersionId && payload.requestedAt;\nconst imageActions = ['regenerate_concept', 'regenerate_base', 'change_template'];\nconst isImageAction = imageActions.includes(payload.action);\nif (!commonValid || (isImageAction ? (!payload.imageStyle || !payload.template) : (!payload.component || !payload.instruction))) throw new Error('Content action contract is invalid');\nconst outbound = isImageAction ? { contractVersion: payload.contractVersion, correlationId: payload.correlationId, idempotencyKey: payload.idempotencyKey, actorId: payload.actorId, brandId: payload.brandId, postDraftId: payload.postDraftId, expectedVersionId: payload.expectedVersionId, requestedAt: payload.requestedAt, action: payload.action, imageStyle: payload.imageStyle, template: payload.template, ...(payload.conceptKey ? { conceptKey: payload.conceptKey } : {}) } : payload;\nconst targetPath = '/api/internal/workflows/recovery/execute';\nconst recoveryPayload = {\n  contractVersion: '1.0',\n  workflowExecutionId: String($execution.id),\n  workflowName: 'WF-09 Content Actions',\n  target: isImageAction ? 'image_generation' : 'content_action',\n  requestPayload: outbound,\n};\nconst recoveryBody = JSON.stringify(recoveryPayload);\nconst nextTimestamp = String(Math.floor(Date.now() / 1000));\nconst nextNonce = crypto.randomUUID();\nconst nextDigest = crypto.createHash('sha256').update(recoveryBody, 'utf8').digest('hex');\nconst nextCanonical = [nextTimestamp, nextNonce, 'POST', targetPath, nextDigest].join('\\n');\nconst signature = `sha256=${crypto.createHmac('sha256', secret).update(nextCanonical, 'utf8').digest('hex')}`;\nreturn [{ json: { body: recoveryBody, nonce: nextNonce, signature, timestamp: nextTimestamp, url: `${$env.APP_BASE_URL}${targetPath}` } }];"
      },
      "id": "bd77f529-4e60-425c-a58c-ba9e77efe903",
      "name": "Verify and Sign Content Action",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [-220, 100]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "accept-encoding",
              "value": "identity"
            },
            {
              "name": "content-type",
              "value": "application/json"
            },
            {
              "name": "x-workflow-name",
              "value": "WF-09 Content Actions"
            },
            {
              "name": "x-workflow-timestamp",
              "value": "={{ $json.timestamp }}"
            },
            {
              "name": "x-workflow-nonce",
              "value": "={{ $json.nonce }}"
            },
            {
              "name": "x-workflow-signature",
              "value": "={{ $json.signature }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ $json.body }}",
        "options": {
          "timeout": 200000
        }
      },
      "id": "bd77f529-4e60-425c-a58c-ba9e77efe904",
      "name": "Persist Selective Content Action",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [20, 100]
    }
  ],
  "pinData": {},
  "connections": {
    "Signed Content Action Webhook": {
      "main": [
        [
          {
            "node": "Verify and Sign Content Action",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verify and Sign Content Action": {
      "main": [
        [
          {
            "node": "Persist Selective Content Action",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": true,
    "executionTimeout": 300
  },
  "versionId": "bd77f529-4e60-425c-a58c-ba9e77efe905",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "tags": []
}

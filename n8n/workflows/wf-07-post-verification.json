{
  "name": "WF-07 Post Verification",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "post-verification-v1",
        "responseMode": "onReceived",
        "options": {
          "responseCode": 202
        }
      },
      "id": "9c55d307-2c48-403a-836a-987c55cdc701",
      "name": "Signed Verification Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [-460, 100],
      "webhookId": "9c55d307-2c48-403a-836a-987c55cdc702"
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst headers = $json.headers || {};\nconst payload = $json.body || {};\nconst timestamp = String(headers['x-workflow-timestamp'] || '');\nconst nonce = String(headers['x-workflow-nonce'] || '');\nconst received = String(headers['x-workflow-signature'] || '');\nconst sourcePath = '/webhook/post-verification-v1';\nconst body = JSON.stringify(payload);\nconst digest = crypto.createHash('sha256').update(body, 'utf8').digest('hex');\nconst canonical = [timestamp, nonce, 'POST', sourcePath, digest].join('\\n');\nconst secret = $env.WORKFLOW_HMAC_SECRET;\nconst previousSecret = $env.WORKFLOW_HMAC_PREVIOUS_SECRET;\nif (!secret || !timestamp || !nonce || !received) throw new Error('Workflow signature is missing');\nconst authentic = [secret, previousSecret].filter(Boolean).some((candidate) => {\n  const expected = `sha256=${crypto.createHmac('sha256', candidate).update(canonical, 'utf8').digest('hex')}`;\n  return received.length === expected.length && crypto.timingSafeEqual(Buffer.from(received), Buffer.from(expected));\n});\nif (!authentic) throw new Error('Workflow signature is invalid');\nif (Math.abs(Math.floor(Date.now() / 1000) - Number(timestamp)) > 300) throw new Error('Workflow signature expired');\nif (payload.contractVersion !== '1.0' || !payload.correlationId || !payload.idempotencyKey || !payload.actorId || !payload.brandId || !payload.postDraftId || !payload.requestedAt) throw new Error('Post verification contract is invalid');\nconst targetPath = '/api/internal/workflows/recovery/execute';\nconst recoveryPayload = {\n  contractVersion: '1.0',\n  workflowExecutionId: String($execution.id),\n  workflowName: 'WF-07 Post Verification',\n  target: 'post_verification',\n  requestPayload: payload,\n};\nconst recoveryBody = JSON.stringify(recoveryPayload);\nconst nextTimestamp = String(Math.floor(Date.now() / 1000));\nconst nextNonce = crypto.randomUUID();\nconst nextDigest = crypto.createHash('sha256').update(recoveryBody, 'utf8').digest('hex');\nconst nextCanonical = [nextTimestamp, nextNonce, 'POST', targetPath, nextDigest].join('\\n');\nconst signature = `sha256=${crypto.createHmac('sha256', secret).update(nextCanonical, 'utf8').digest('hex')}`;\nreturn [{ json: { body: recoveryBody, nonce: nextNonce, signature, timestamp: nextTimestamp, url: `${$env.APP_BASE_URL}${targetPath}` } }];"
      },
      "id": "9c55d307-2c48-403a-836a-987c55cdc703",
      "name": "Verify and Sign Verification Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [-220, 100]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "accept-encoding",
              "value": "identity"
            },
            {
              "name": "content-type",
              "value": "application/json"
            },
            {
              "name": "x-workflow-name",
              "value": "WF-07 Post Verification"
            },
            {
              "name": "x-workflow-timestamp",
              "value": "={{ $json.timestamp }}"
            },
            {
              "name": "x-workflow-nonce",
              "value": "={{ $json.nonce }}"
            },
            {
              "name": "x-workflow-signature",
              "value": "={{ $json.signature }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ $json.body }}",
        "options": {
          "timeout": 200000
        }
      },
      "id": "9c55d307-2c48-403a-836a-987c55cdc704",
      "name": "Reevaluate and Persist Readiness",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [20, 100]
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst verification = $input.first().json;\nconst request = $('Signed Verification Webhook').first().json.body;\nif (verification?.contractVersion !== '1.0' || verification.postDraftId !== request.postDraftId || !verification.postVersionId || !verification.evaluation || typeof verification.evaluation.readyForReview !== 'boolean') throw new Error('Verification result contract is invalid');\nconst secret = $env.WORKFLOW_HMAC_SECRET;\nconst baseUrl = $env.N8N_WEBHOOK_BASE_URL;\nif (!secret || !baseUrl) throw new Error('Image environment is not configured');\nconst path = '/webhook/image-generation-v1';\nconst payload = {\n  contractVersion: '1.0',\n  correlationId: request.correlationId,\n  idempotencyKey: `verification-auto-image:${request.postDraftId}:${verification.postVersionId}`,\n  actorId: request.actorId,\n  brandId: request.brandId,\n  postDraftId: request.postDraftId,\n  expectedVersionId: verification.postVersionId,\n  action: 'generate',\n  imageStyle: 'editorial_hero',\n  template: 'editorial_overlay',\n  requestedAt: new Date().toISOString(),\n};\nconst body = JSON.stringify(payload);\nconst timestamp = String(Math.floor(Date.now() / 1000));\nconst nonce = crypto.randomUUID();\nconst digest = crypto.createHash('sha256').update(body, 'utf8').digest('hex');\nconst canonical = [timestamp, nonce, 'POST', path, digest].join('\\n');\nconst signature = `sha256=${crypto.createHmac('sha256', secret).update(canonical, 'utf8').digest('hex')}`;\nreturn [{ json: { dispatch: true, body, nonce, signature, timestamp, url: `${baseUrl}${path}` } }];"
      },
      "id": "9c55d307-2c48-403a-836a-987c55cdc706",
      "name": "Sign Image Handoff",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [260, 100]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "c070f001-6a77-4a0f-8d1c-000000000006",
              "leftValue": "={{ $json.dispatch }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "c070f001-6a77-4a0f-8d1c-000000000006",
      "name": "Dispatch Image Generation?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [500, 100]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "accept-encoding",
              "value": "identity"
            },
            {
              "name": "content-type",
              "value": "application/json"
            },
            {
              "name": "x-workflow-name",
              "value": "WF-07 Post Verification"
            },
            {
              "name": "x-workflow-timestamp",
              "value": "={{ $json.timestamp }}"
            },
            {
              "name": "x-workflow-nonce",
              "value": "={{ $json.nonce }}"
            },
            {
              "name": "x-workflow-signature",
              "value": "={{ $json.signature }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ $json.body }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": false
            }
          },
          "timeout": 30000
        }
      },
      "id": "9c55d307-2c48-403a-836a-987c55cdc707",
      "name": "Dispatch Image Handoff",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [740, 100]
    }
  ],
  "pinData": {},
  "connections": {
    "Signed Verification Webhook": {
      "main": [
        [
          {
            "node": "Verify and Sign Verification Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verify and Sign Verification Request": {
      "main": [
        [
          {
            "node": "Reevaluate and Persist Readiness",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Reevaluate and Persist Readiness": {
      "main": [
        [
          {
            "node": "Sign Image Handoff",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sign Image Handoff": {
      "main": [
        [
          {
            "node": "Dispatch Image Generation?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Dispatch Image Generation?": {
      "main": [
        [
          {
            "node": "Dispatch Image Handoff",
            "type": "main",
            "index": 0
          }
        ],
        []
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": true,
    "executionTimeout": 300
  },
  "versionId": "9c55d307-2c48-403a-836a-987c55cdc705",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "tags": []
}
